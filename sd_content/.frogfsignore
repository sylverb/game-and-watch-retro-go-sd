# Patterns (fnmatch, relative to roms/) excluded from the FrogFS image of
# SD_CARD=0 builds. Files stay in sd_content for the SD sync flow.
#
# Example -- exclude the FF4 J2e variant (2 MiB, ADR-008) when building
# with EXTFLASH_SIZE_MB=4 (it does not fit next to the vanilla ROM; the
# bench-validated dual-language config bakes BOTH at EXTFLASH_SIZE_MB=8):
# homebrew/ff4-j2e.sfc
